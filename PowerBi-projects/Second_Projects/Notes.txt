### LOAD Sales CSV
# Uncheck right click and disable the enable load  
# help me in building data model


# reference a table kraken koffee fact(fact table)
# remove store location
# only store id with other columns   


StoreDIM
# reference table again store dim (dimensional table) stored_id , store_location , remove other columns 
# remove duplicates from left corner

Products dim
# reference table again products (all products this time and remove other columns)
# remove duplicates 

 # go again to kraken fact table to keep product it to model with dimensional table of products
    

## Add new columns to kraken koffee fact 

transactionqty unitprice add column standard  (X multiply) === Rename Total Sales


transform will replace 
Add column will add 
> transaction Time --> Time add column hour 


DATE
# Reference again 
select transactionate only other remove
> remove duplicates
> Add column Date , Month (numeric _) , Month Name , Add column Extract First 3 Character of Month Name (Month Name Short)

# Add column Day of the week 

# Add column Date --> Add Day Name , Extract first 3 Characters Day Name Short

Close and Apply


########### MODELLING 

Product_Dim  (1 to many ) kraken koffee fact
Store_Dim (1 to many) kraken koffee Fact
Date transaction date to transaction date of kraken koffee fact

###  DAX -> Data analysis expression 

Go to power Query Enter 
New table create _01_Core_Measures(Basic Dax)

> Create reference table 
_02_Nested_Measures                  ]  DISABLE report refresh

> _03_Formatting_and_Text
  



## SUMMARY DATA new Page
01_Core_measures --> Total Transaction = distinctcount(kraken_koffee_fact[transaction_id])  > display units None

Total Transaction = distinctcount(kraken_koffee_fact[transaction_id])  UNIQUE
Total Transaction = count(kraken_koffee_fact[transaction_id])  NOT UNIQUE
# Hide column1 

new measures --> total Sales = SUM(Kraken_Koffee_Fact[Total Sales]) format currency and 0 round 
Average Order Size = Average(kraken_koffee_Fact(Total Sales]) average order size 
    | | SAME
Average Order Size = DIVIDE([Total Sales],[Total Transactions])

Total Stores = DISTINCTCOUNT(Store_Dim[store_id])

Total Products = Count(product_Dim[product_id])


## More complex measures

product detail  Total Sales , Total Transaction
product id 


Product Sales Rank = RANKX(values(Product_Dim[Product_detail]),[Total Sales],,desc,  Dense)
Product Sales Rank = RANKX(ALLSELECTED(Product_Dim[Product_detail]),[Total Sales],,desc,  Dense)
Product Sales Rank = RANKX(ALLSELECTED(Product_Dim),[Total Sales],,desc,  Dense) # rank everything from any column if you have product id
All selected we are trying to rank all the values each other

rankx resutrns the rank of an expression evaluated in the current context in the list of values for the expression evaluated for each row
in the specified table

Product Transactions Rank = RANKX(ALLSELECTED(Product_Dim[Product_detail]),[Total Sales],,desc,  Dense)
Total sales Total transactions Product Sales Rank Product detail Product Transaction Rank

Top Earner = CALCULATE(min(Product_Dim[product_detail]),filter(Product_Dim,[Product Sales Rank]=1))
Most Popular Seller = CALCULATE(min(Product_Dim[product_detail]),filter(Product_Dim,[Product Transactions Rank]=1))


## Reporting 
Columns  Product Detail, total Sales
Prodcut details advance filtering Top N Top 10 TOTAL sALES

